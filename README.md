# AWS CLI Access to SeaweedFS S3 on Local Kubernetes (LoadBalancer)

This repository contains a public runbook documenting how AWS CLI access was set up for a SeaweedFS S3 service running on a local Kubernetes cluster.

- Full runbook: `docs/awscli-seaweedfs-s3-access.md`
